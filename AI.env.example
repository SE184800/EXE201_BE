# Chép hai dòng này vào .env của BACKEND, không phải frontend.
# Điền key riêng trên máy bạn; không gửi key qua chat hoặc commit Git.
OPENAI_API_KEY=
# Model hỗ trợ Responses API mà tài khoản của bạn có quyền dùng.
OPENAI_MODEL=
